PORT=5000
# Atlas가 있으면 우선 사용. 비어 있으면 MONGODB_URI(로컬) 사용.
MONGODB_ATLAS_URI=
MONGODB_URI=mongodb://127.0.0.1:27017/shopping-mall
NODE_ENV=development
JWT_SECRET=shopping-mall-dev-secret-change-me
JWT_EXPIRES_IN=7d

# PortOne V2 — payment confirmation (never commit real secrets)
PORTONE_API_SECRET=
